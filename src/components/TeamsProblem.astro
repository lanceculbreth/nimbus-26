---
import img1 from '../assets/[4] Product Imagery & Video/iStock-2187968525.jpg';
import img2 from '../assets/[4] Product Imagery & Video/iStock-1276569663.jpg';
import img3 from '../assets/[4] Product Imagery & Video/iStock-2189538504.jpg';
import img4 from '../assets/[4] Product Imagery & Video/iStock-1317735408.jpg';

const cards = [
  {
    image: img1.src,
    title: 'Unpredictable Drug Spend',
    description: 'Dose-based cost escalation tied to branded medications creates budget volatility that\'s impossible to forecast.',
  },
  {
    image: img2.src,
    title: 'High Drop-Off & Restart Rates',
    description: 'Without integrated clinical support, employees cycle through starts and stops that waste spend and undermine outcomes.',
  },
  {
    image: img3.src,
    title: 'Fragmented Care',
    description: 'PBMs, telehealth vendors, and nutrition programs operate in silos with no single accountable owner for results.',
  },
  {
    image: img4.src,
    title: 'No Outcome Accountability',
    description: 'Employers pay for medication volume — not medication value — with no clear line to adherence or persistence.',
  },
];
---

<section class="bg-cream py-16 md:py-24 overflow-hidden" data-animate>
  <!-- Header -->
  <div id="teams-problem-header" class="max-w-[1200px] mx-auto px-5 md:px-[10px] mb-10 md:mb-16">
    <p class="font-inter text-dark/50 text-base mb-3">The problem</p>
    <h2 class="font-fraunces text-dark text-[clamp(32px,5vw,72px)] leading-[1.05] tracking-[-0.02em]">
      Traditional isn't working
    </h2>
  </div>

  <!-- Carousel -->
  <div class="relative overflow-hidden">
    <div id="teams-problem-track" class="flex gap-4 md:gap-6 transition-transform duration-500 ease-in-out">
      {cards.map((card) => (
        <div class="teams-problem-slide shrink-0 w-[calc(100vw-40px)] md:w-[500px]">
          <div class="relative rounded-2xl h-[350px] md:h-[500px] overflow-hidden mb-4 md:mb-5">
            <img src={card.image} alt="" class="w-full h-full object-cover" />
            <div class="absolute inset-0 bg-dark/10"></div>
          </div>
          <h3 class="font-inter text-dark text-base md:text-lg font-bold mb-2 px-1">{card.title}</h3>
          <p class="font-inter text-dark/50 text-sm leading-[1.6] px-1">{card.description}</p>
        </div>
      ))}
    </div>
  </div>

  <!-- Controls -->
  <div class="flex items-center justify-end gap-4 mt-8 max-w-[1200px] mx-auto px-5 md:px-[10px]">
    <button
      id="teams-problem-prev"
      class="w-10 h-10 rounded-full bg-dark/10 flex items-center justify-center text-dark/40 hover:text-dark hover:bg-dark/20 transition-colors cursor-pointer"
      aria-label="Previous slide"
    >
      <svg width="18" height="18" viewBox="0 0 18 18" fill="none">
        <path d="M11 4L6 9l5 5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    </button>

    <button
      id="teams-problem-next"
      class="w-10 h-10 rounded-full bg-dark/10 flex items-center justify-center text-dark/40 hover:text-dark hover:bg-dark/20 transition-colors cursor-pointer"
      aria-label="Next slide"
    >
      <svg width="18" height="18" viewBox="0 0 18 18" fill="none">
        <path d="M7 4l5 5-5 5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    </button>
  </div>

  <!-- Bottom Line Banner -->
  <div class="max-w-[1200px] mx-auto px-5 md:px-[10px] mt-10">
    <div class="bg-teal rounded-2xl px-6 md:px-10 py-8 md:py-10">
      <p class="font-inter text-cream text-base md:text-lg leading-[1.6]">
        <span class="font-bold">Bottom line:</span> Employers are paying for medication volume, not medication value, with limited cost predictability.
      </p>
    </div>
  </div>
</section>

<script is:inline>
  (function() {
    var track = document.getElementById('teams-problem-track');
    var header = document.getElementById('teams-problem-header');
    var prevBtn = document.getElementById('teams-problem-prev');
    var nextBtn = document.getElementById('teams-problem-next');
    var current = 0;
    var total = 4;

    function getSlideMetrics() {
      var slide = track.querySelector('.teams-problem-slide');
      if (!slide) return { cardWidth: 500, gap: 24 };
      var w = slide.getBoundingClientRect().width;
      var g = parseFloat(getComputedStyle(track).gap) || 24;
      return { cardWidth: w, gap: g };
    }

    function getLeftStart() {
      if (header) {
        var rect = header.getBoundingClientRect();
        var padding = parseFloat(getComputedStyle(header).paddingLeft);
        return rect.left + padding;
      }
      return 20;
    }

    function getOffset(index) {
      var m = getSlideMetrics();
      return -(index * (m.cardWidth + m.gap)) + getLeftStart();
    }

    function goTo(index) {
      if (index < 0) index = total - 1;
      if (index > total - 1) index = 0;
      current = index;
      track.style.transform = 'translateX(' + getOffset(current) + 'px)';
    }

    track.style.transition = 'none';
    track.style.transform = 'translateX(' + getLeftStart() + 'px)';
    requestAnimationFrame(function() {
      requestAnimationFrame(function() {
        track.style.transition = '';
      });
    });

    prevBtn?.addEventListener('click', function() { goTo(current - 1); });
    nextBtn?.addEventListener('click', function() { goTo(current + 1); });

    // Touch swipe
    var touchStartX = 0;
    var touchStartY = 0;
    var touchDeltaX = 0;
    var isSwiping = false;
    var swipeThreshold = 50;

    track.addEventListener('touchstart', function(e) {
      touchStartX = e.touches[0].clientX;
      touchStartY = e.touches[0].clientY;
      touchDeltaX = 0;
      isSwiping = false;
      track.style.transition = 'none';
    }, { passive: true });

    track.addEventListener('touchmove', function(e) {
      var dx = e.touches[0].clientX - touchStartX;
      var dy = e.touches[0].clientY - touchStartY;
      if (!isSwiping && Math.abs(dx) > Math.abs(dy) && Math.abs(dx) > 10) {
        isSwiping = true;
      }
      if (isSwiping) {
        e.preventDefault();
        touchDeltaX = dx;
        track.style.transform = 'translateX(' + (getOffset(current) + touchDeltaX) + 'px)';
      }
    }, { passive: false });

    track.addEventListener('touchend', function() {
      track.style.transition = 'transform 0.5s ease-in-out';
      if (isSwiping) {
        if (touchDeltaX < -swipeThreshold) {
          goTo(current + 1);
        } else if (touchDeltaX > swipeThreshold) {
          goTo(current - 1);
        } else {
          track.style.transform = 'translateX(' + getOffset(current) + 'px)';
        }
      }
      isSwiping = false;
      touchDeltaX = 0;
    }, { passive: true });

    window.addEventListener('resize', function() {
      track.style.transition = 'none';
      track.style.transform = 'translateX(' + getOffset(current) + 'px)';
      requestAnimationFrame(function() {
        requestAnimationFrame(function() {
          track.style.transition = '';
        });
      });
    });
  })();
</script>
